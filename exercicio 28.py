número = []*10
t = len(numeros)
maior=0
menor=10000000000

for x in range(t)
numeros[x]=int(imput("Digite um número: "))

for y in range(t):
    if numeros[y] % 2 == maior:
        maior=numeros[y]
        